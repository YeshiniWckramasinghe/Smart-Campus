spring.application.name=smart-campus

spring.profiles.active=local

# --- Aiven MySQL (local profile) ---
# IMPORTANT: Aiven requires a database name in the URL path.
# Set these as environment variables before starting the backend:
#   AIVEN_DB_NAME (your database name)
#   AIVEN_DB_PASSWORD (avnadmin password)
spring.datasource.url=jdbc:mysql://mysql-16b60287-yasadapoorna-b1bb.c.aivencloud.com:11635/?user=avnadmin
spring.datasource.username=avnadmin
spring.datasource.password=${AVNS_BpF9QN3bXe-BIBIwkTI}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile

jwt.secret=smartcampus_secret_key_2024_sliit_very_long_key_256bits
jwt.expiration=86400000

server.port=8082

spring.main.allow-circular-references=true
